{
  "name": "node-backup",
  "version": "1.0.0",
  "description": "Create backups of a folder with a single command.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "backup"
  ],
  "author": "smc",
  "license": "ISC",
  "dependencies": {
    "node-run-cmd": "^1.0.1"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-jshint": "^2.1.0",
    "jshint": "^2.9.5"
  }
}
